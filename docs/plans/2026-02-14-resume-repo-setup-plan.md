# Resume Repo Setup Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Set up the `yamlresume` repo with tooling mirroring `yamlresume-lianne` and populate it with Shane's resume content.

**Architecture:** Single `resume.yml` using yamlresume schema, built via yamlresume CLI + xelatex. Dev tooling via npm scripts, commitlint, husky, lint-staged, prettier, eslint.

**Tech Stack:** yamlresume, xelatex, Node.js (volta 25.2.1), husky, commitlint, eslint, prettier

---

### Task 1: Create .gitignore

**Files:**
- Create: `.gitignore`

**Step 1: Create .gitignore**

Copy from `yamlresume-lianne`. This is the standard Node.js gitignore plus yamlresume build artifacts.

```gitignore
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-woven dependency directory
node_modules/

# snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test
.env.production

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# YAMLResume build output
*.aux
*.out
*.toc
*.lof
*.lot
*.fls
*.tex
*.pdf
```

**Step 2: Commit**

```bash
git add .gitignore
git commit -m "chore: add .gitignore"
```

---

### Task 2: Create package.json and install dependencies

**Files:**
- Create: `package.json`

**Step 1: Create package.json**

Mirrors `yamlresume-lianne` but without the `build:theme-tweaks` step, and updated name/description.

```json
{
  "name": "yamlresume",
  "version": "1.0.0",
  "description": "YAMLResume project for Shane Martin",
  "scripts": {
    "build:tex": "yamlresume build --no-pdf resume.yml",
    "build:pdf": "xelatex resume.tex",
    "build": "npm run build:tex && npm run build:pdf",
    "dev": "yamlresume dev resume.yml",
    "validate": "yamlresume validate resume.yml",
    "yamlresume": "yamlresume",
    "prepare": "husky",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx --fix",
    "format": "prettier --write ."
  },
  "keywords": [
    "YAMLResume",
    "Resume",
    "CV"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "yamlresume": "latest"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "eslint": "^9.39.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "sort-package-json": "^3.6.0"
  },
  "volta": {
    "node": "25.2.1"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,css,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
```

**Step 2: Install dependencies**

```bash
npm install
```

Expected: `node_modules/` created, `package-lock.json` generated.

**Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add package.json and install dependencies"
```

---

### Task 3: Set up commitlint and husky

**Files:**
- Create: `commitlint.config.cjs`
- Create: `.husky/commit-msg`
- Create: `.husky/pre-commit`

**Step 1: Create commitlint config**

```javascript
module.exports = {
  extends: ['@commitlint/config-conventional']
};
```

**Step 2: Initialize husky**

```bash
npx husky init
```

**Step 3: Create commit-msg hook**

Write `.husky/commit-msg` with contents:

```bash
npx commitlint --edit "$1"
```

**Step 4: Create pre-commit hook**

Write `.husky/pre-commit` with contents:

```bash
npx lint-staged
```

**Step 5: Commit**

```bash
git add commitlint.config.cjs .husky/
git commit -m "chore: set up commitlint and husky hooks"
```

---

### Task 4: Add install-tex script

**Files:**
- Create: `scripts/install-tex.sh`

**Step 1: Create the script**

```bash
# borrowed from https://algrt.hm/2024-07-22-latex-on-macos/ (plus some tweaks)
# brew
brew install basictex

# reload path
eval "$(/usr/libexec/path_helper)"

# tex bits
sudo tlmgr update --self
sudo tlmgr install \
    texliveonfly \
    adjustbox \
    tcolorbox \
    collectbox \
    ucs \
    environ \
    trimspaces \
    titling \
    enumitem \
    rsfs \
    moderncv \
    ctex
```

**Step 2: Commit**

```bash
git add scripts/install-tex.sh
git commit -m "chore: add LaTeX install script"
```

---

### Task 5: Create resume.yml — basics, location, profiles

**Files:**
- Create: `resume.yml`

**Step 1: Create resume.yml with header, basics, location, and profiles**

This is the first portion of the resume. We use a placeholder for the summary that will be refined later. Contact details come from `src/old-yaml/bio_data_base.yaml`.

```yaml
# yaml-language-server: $schema=https://yamlresume.dev/schema.json

---
content:
  basics:
    name: Shane Martin
    headline: Software Developer & Front-End Architect
    phone: 'REDACTED'
    email: sh@nemart.in
    summary: |
      - Experienced software developer with 18+ years building for the web, specializing in front-end architecture, design systems, developer experience, and engineering leadership.
      - Deep expertise in modernizing legacy codebases, building component libraries, and establishing robust development standards and tooling that help teams deliver better software faster.
      - Background in research methods and data analysis from a Specialized Honours Psychology degree at York University.

  location:
    city: Toronto
    region: Ontario
    country: Canada
  profiles:
    - network: LinkedIn
      url: https://www.linkedin.com/in/shamrt/
      username: shamrt
    - network: GitHub
      url: https://github.com/shamrt
      username: shamrt
```

**Step 2: Validate**

```bash
npm run validate
```

Expected: Validation passes (or warns about missing sections, which is fine at this stage).

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add resume basics, location, and profiles"
```

---

### Task 6: Add education section

**Files:**
- Modify: `resume.yml`

**Step 1: Add education section after profiles**

Append to the `content:` block in `resume.yml`, after `profiles`:

```yaml
  education:
    - institution: York University
      degree: Bachelor
      area: Psychology (Specialized Honours)
      score: '8.63/9.00'
      startDate: Sep 1, 2010
      endDate: Apr 30, 2015
      summary: |
        Graduated summa cum laude. Research focused on meta-cognition, digital history, research methods, statistics, and data analysis.
```

**Step 2: Validate**

```bash
npm run validate
```

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add education section"
```

---

### Task 7: Add work experience — RBC

**Files:**
- Modify: `resume.yml`

**Step 1: Add work section with RBC role**

Add `work:` array to `content:` block. Start with the oldest role; we'll prepend newer roles in subsequent tasks. Content adapted from `src/old-yaml/experience_tech.yaml`.

```yaml
  work:
    - name: RBC
      startDate: Oct 2016
      endDate: Feb 2019
      position: Senior Front-End Developer
      summary: |
        Developed and maintained Angular apps for various online banking modernization projects within RBC's digital transformation initiative.

        - **Created RBC Digital Frameworks component library** (POC) based on existing design patterns, with full unit, E2E, visual regression, and accessibility test coverage
        - **Developed and maintained core web libraries** including the RBC web icons and styling (Sass) libraries, both fully tested
        - **Advocated for development standards and best practices** including automated testing, web accessibility, linting, git tooling (code reviews, conventional commits, semver), monorepo architecture, and CI/CD
        - **Mentored junior developers and co-op students**, working closely with them on code quality and professional development
```

**Step 2: Validate**

```bash
npm run validate
```

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add RBC work experience"
```

---

### Task 8: Add work experience — RewardOps (4 roles)

**Files:**
- Modify: `resume.yml`

**Step 1: Add RewardOps roles**

Prepend four RewardOps entries before the RBC entry in the `work:` array. These represent the career progression from Senior Developer through Front-end Architect. Content adapted from `src/old-yaml/experience_tech.yaml`.

```yaml
    - name: RewardOps
      startDate: Jan 2022
      endDate: Jan 2023
      position: Front-end Architect
      summary: |
        Led front-end architecture evolution across the platform, driving major modernization initiatives and developer experience improvements.

        - **Led development of a new React app** on a tight timeline: major feature development, E2E and mocking frameworks, partner SSO integration
        - **Led front-end re-architecture** increasing Jest test speed 2X, optimizing build bundles, and converting from Webpack to Vite (30X dev build time improvement)
        - **Managed RushJS monorepo** including DX enhancements, automations (git hooks), re-wrote tooling, and eliminated over 95% of noise in Jest test suites
        - **Introduced Redux Toolkit Query** for data fetching, eliminating 90% of boilerplate code
        - **Combined Node server applications**, eliminating over 30K lines of superfluous code

    - name: RewardOps
      startDate: Jun 2019
      endDate: May 2022
      position: Software Development Manager
      summary: |
        Built and managed the development team while maintaining hands-on technical leadership and driving product quality improvements.

        - **Built an entire dev team from scratch** managing up to 11 direct reports with biweekly 1-on-1s, mentoring, and team advocacy
        - **Partnered with Product and stakeholders** to maximize product quality, improve feedback loops, and encourage product participation
        - **Mentored other team leads and managers** while championing knowledge dissemination, documentation, and learning culture
        - **Drove technical direction** through code/design reviews, RFCs, architecture documentation, roadmap planning, hiring, and onboarding

    - name: RewardOps
      startDate: Jun 2019
      endDate: Jan 2022
      position: Lead Software Developer
      summary: |
        Technical lead driving the Admin Console project and key product launches while modernizing the front-end stack.

        - **Led technical design and implementation of MXaaS** (Member Experience as a Service) from inception to product launch in 2020
        - **Shepherded Ember-to-React transition** including modernizing a 3-years-out-of-date Ember library and developing integration tools
        - **Improved Ember test suite throughput 5X** (7 min to 1.5 min) and cold/warm boot times 2X
        - **Overhauled core Node SDK library** and guided multiple large Ember app and addon upgrades including transition to Ember Octane

    - name: RewardOps
      startDate: Mar 2019
      endDate: Jun 2019
      position: Senior Software Developer
      summary: |
        Contributed features and drove coding standards improvements across the front-end codebase.

        - **Doubled Ember test suite speed** (from 11 to under 7 minutes) and added code coverage
        - **Integrated rigorous coding standards** into SDLC: linting, formatting, git hooks, inline documentation, CI pipeline checks including SonarCloud
        - **Successfully campaigned to unify the company around React** as the front-end development framework
```

**Step 2: Validate**

```bash
npm run validate
```

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add RewardOps work experience (4 roles)"
```

---

### Task 9: Add work experience — Plooto

**Files:**
- Modify: `resume.yml`

**Step 1: Add Plooto entry**

Prepend before the RewardOps entries in the `work:` array. Content adapted from `src/old-yaml/experience_tech.yaml`.

```yaml
    - name: Plooto Inc.
      startDate: Jan 2023
      endDate: Mar 2024
      position: Staff Front-end Developer
      summary: |
        Overhauled and modernized the front-end architecture of a 9-year-old Durandal (Knockout) application as part of a comprehensive platform modernization effort.

        - **Led migration to React** including introducing modern build tooling (Webpack, code-splitting, tree-shaking), a new component library (Storybook + MUI), and standardized patterns for i18n, data fetching, and config management
        - **Grew test coverage from ~2% to over 30%** via new testing standards, guardrails, and developer experience enhancements
        - **Reduced CI pipeline runtime from 10+ minutes to 5 minutes** despite adding many new checks, using caching, parallelization, and test suite optimizations
        - **Reorganized codebase** using Yarn workspaces and Bulletproof architecture
        - **Wrote RFCs, guidelines, and roadmaps** to guide Engineering team technical direction
```

**Step 2: Validate**

```bash
npm run validate
```

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add Plooto work experience"
```

---

### Task 10: Add work experience — Vantage Inc. (placeholder)

**Files:**
- Modify: `resume.yml`

**Step 1: Add Vantage placeholder**

Prepend a minimal entry at the top of the `work:` array. The user will provide full details later.

```yaml
    - name: Vantage Inc.
      startDate: Apr 2024
      endDate:
      position: TBD
      summary: |
        Details to be provided.
```

**Step 2: Validate**

```bash
npm run validate
```

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add Vantage Inc. placeholder"
```

---

### Task 11: Add skills section

**Files:**
- Modify: `resume.yml`

**Step 1: Add skills section**

Add after `work:` in the `content:` block. Single unnamed skill group with flat keyword list, matching `yamlresume-lianne` format.

```yaml
  skills:
    - name:
      keywords:
        - JavaScript
        - TypeScript
        - Node
        - React
        - Redux
        - Ember
        - Angular
        - Ruby
        - Python
        - R
        - C
        - TDD/BDD
        - Jest
        - React Testing Library
        - Playwright
        - Cypress
        - Storybook
        - Webpack
        - Vite
        - Monorepo Tooling
        - CI/CD
        - Docker
        - Git
        - PostgreSQL
        - MySQL
        - MongoDB
        - SQLite
        - CSS-in-JS
        - Sass
        - Semantic HTML
        - Accessibility
        - REST APIs
        - System Architecture
        - Technical Leadership
        - Mentoring
```

**Step 2: Validate**

```bash
npm run validate
```

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add skills section"
```

---

### Task 12: Add layout configuration

**Files:**
- Modify: `resume.yml`

**Step 1: Add locale and layouts sections**

Append after the `content:` block (at root level):

```yaml
locale:
  language: en

layouts:
  - engine: latex
    page:
      margins:
        top: 2.5cm
        left: 1.5cm
        right: 1.5cm
        bottom: 2.5cm
      showPageNumbers: false
    template: moderncv-casual
    typography:
      fontSize: 11pt
    sections:
      aliases:
        basics: Summary
        work: Experience
      order:
        - basics
        - work
        - education
        - skills
```

**Step 2: Validate**

```bash
npm run validate
```

Expected: Full validation pass with all sections present.

**Step 3: Commit**

```bash
git add resume.yml
git commit -m "feat: add layout configuration"
```

---

### Task 13: Test build pipeline

**Step 1: Build tex**

```bash
npm run build:tex
```

Expected: `resume.tex` generated without errors.

**Step 2: Build PDF**

```bash
npm run build:pdf
```

Expected: `resume.pdf` generated. If xelatex is not installed, run `sh scripts/install-tex.sh` first.

**Step 3: Visual check**

Open `resume.pdf` and verify it looks correct — sections in order, content readable, no layout issues.

**Step 4: Clean up build artifacts**

```bash
rm -f resume.tex resume.pdf resume.aux resume.log resume.out
```

**Step 5: Commit (if any fixes were needed)**

Only commit if changes were required to fix build issues.
